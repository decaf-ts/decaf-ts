# DECAF-51: with-ai MCP/CLI — mcp-server Redesign and Port

**Status:** Completed — all acceptance criteria have outcomes (§2, §7); delivery finalized 2026-09-04 through the completion milestone on domain root [SAA-560](/SAA/issues/SAA-560), and the user's reopened-scope items 1–7 (2026-09-05) are delivered, verified, and security-gated ([SAA-728](/SAA/issues/SAA-728) PASS) — folded into §7 on 2026-09-05 under [SAA-729](/SAA/issues/SAA-729) (Paperclip is authoritative for lifecycle; this line is a snapshot as of the latest milestone). The first single user-approved commit landed 2026-09-05 (repo root `293ab32` + with-ai submodule pin `c9fde50`, not pushed — §7), and the reopened-scope low-priority pair ([SAA-732](/SAA/issues/SAA-732)/[SAA-733](/SAA/issues/SAA-733)) is delivered uncommitted pending the next commit gate; the root reaches its terminal state after that next commit (or a user-declined commit — the work is complete and verified either way).
**Priority:** Medium
**Owner:** CTO / Engineering (decaf-ts `with-ai`)

> **Approval provenance.** Product scope was approved by the Product Manager on 2026-09-02 (the verbatim user brief on the domain root is the approved scope statement; goals, non-goals, and user stories confirmed as summarized below; legacy non-Jira module disposition assumed dropped from the MCP surface pending the user's answer, with capability-level coverage validation required; product ruling: "no functionality lost" is measured at the **capability level**, not the MCP tool level — dropped tools are acceptable only where with-ai's catalog + orchestration demonstrably cover the capability; four assumptions flagged as unresolved questions pending the user's interaction answers). Technical governance / architecture was approved by the CTO on 2026-09-02 (architecture direction for the CLI/MCP split and per-module self-registration; stderr-only MCP logging under stdio transport; latest MCP SDK with new transports evaluated; multi-project Jira access; `@decaf-ts/crypto`-based build-time encryption replacing raw node crypto; skill installation via symlinks to decrypted md files; feasibility confirmed against a direct repo scan of `mcp-server/` v1.15.2 (~187 TS files, ~431 asset files) and `with-ai/` v3.10.17 (minimal TS surface, authoritative markdown catalog); binding security and testing constraints; three conditions recorded in §4). The verbatim approved statements remain on their source approval issues; implementation is delegated by the domain-root owner under separate children, each carrying specification `DECAF-51`. A user hard requirement added 2026-09-02 is recorded as first-class: **the MCP tooling must boot clients per request, so credentials are lazily loaded and never break boot.** A user-approved scope amendment (2026-09-02 comment on the domain root) is recorded as first-class: **no commands and no prompts in with-ai** — the MCP module surface becomes tools/templates only (US-8, Req-1, Req-2 amended), ticket templates are single-sourced from the skills catalog assets with no workdocs duplicates (Req-3 amended), `with-ai/workdocs/prompts/**` and all of `with-ai/workdocs/templates/**` are removed from the repository (removal implemented under [SAA-614](/SAA/issues/SAA-614); live `with-ai/agents/` and `with-ai/skills/` untouched), and legacy prompt/command modules are dropped, not ported — capability coverage is provided by the with-ai skills catalog plus Paperclip orchestration, consistent with the capability-level loss-measurement rule.

## 1. Overview

This specification retires decaf's `./mcp-server` package and redesigns its entire capability surface into the `./with-ai` decaf module, which is already the authoritative source for decaf skills, agents, and markdown content. The TypeScript functionality of mcp-server becomes decaf-cli commands (`decaf <command> ...`); the MCP server boot becomes its own fully detached CLI command exposing only jira/xray tooling plus common tooling not already replaced by existing skills and orchestration. Nothing user-facing is lost in the port — only improved — and loss is measured at the capability level: where a legacy tool is dropped from the MCP surface, with-ai's catalog plus orchestration must demonstrably cover the capability, validated as part of the port. Per the user-approved scope amendment of 2026-09-02, with-ai offers **no commands and no prompts**: the MCP module surface is tools/templates only, and the legacy `workdocs` prompts/templates are removed from the repository rather than ported; dropped prompt/command capabilities are covered by the with-ai skills catalog plus Paperclip orchestration.

## 2. Goals

*   [x] Retire `./mcp-server`; all its user-facing capability lives on in `./with-ai` — nothing lost, only improved. *(Outcome: `@decaf-ts/mcp-server` retired — removed from the workspace submodule manifest; its full capability surface ported or explicitly dispositioned per the Req-8 inventory. The npm deprecation/publish disposition of the legacy package name remains an open operational question, §6.)*
*   [x] `with-ai` remains the single authoritative source for skills/agents/markdown; its coverage of everything mcp-server had is **validated**, not assumed. *(Outcome: 100% legacy-surface inventory delivered at `with-ai/workdocs/legacy-inventory.md` (Phase 1, [SAA-566](/SAA/issues/SAA-566)) — every legacy tool/prompt/resource/module/asset has an explicit disposition; validation result: "no uncovered capability remains".)*
*   [x] All with-ai TypeScript functionality becomes decaf-cli commands (auto-picked-up cli modules, invoked as `decaf <command> ...`). *(Outcome: `with-ai/src/cli/commands` — `mcp`, `encrypt-assets`, `install-skills`; Phase 2 [SAA-567](/SAA/issues/SAA-567).)*
*   [x] MCP boot is its own detached CLI command; MCP code fully separated from the rest of the CLI (no shared process-lifecycle state; MCP code must not import CLI command modules). *(Outcome: detached boot in `with-ai/src/mcp/McpBoot.ts` (`decaf with-ai mcp` / `with-ai-mcp` bin); MCP code does not import CLI command modules.)*
*   [x] MCP exposes only jira/xray tooling plus common tooling not already replaced by skills/orchestration, under the strict layout `src/mcp/{jira,xray,common}` with per-module self-registration of tools/templates only — no prompts (amended 2026-09-02). *(Outcome: `src/mcp/{jira,xray,common}` with `@mcpModule()` self-registration through `McpModuleRegistry` — 21 jira tools, 4 xray step tools, common tooling; prompts surface removed down to `McpServerService` capabilities; Phases 3–5 [SAA-568](/SAA/issues/SAA-568)/[SAA-569](/SAA/issues/SAA-569)/[SAA-570](/SAA/issues/SAA-570).)*
*   [x] **Hard requirement (user, 2026-09-02):** the MCP tooling boots clients **per request** — Jira/Xray clients are constructed per tool request with lazily-loaded credentials; missing or invalid credentials must never break MCP server boot. *(Outcome: honored and test-pinned — `RequestClientFactory` constructs clients per tool request; boot reads no credentials; missing/invalid credentials never break boot.)*
*   [x] Upgrade to the latest MCP SDK core dependencies; evaluate and include newly available transports/features (e.g. Streamable HTTP). *(Outcome: latest MCP SDK with Streamable HTTP transport; hardened in fix round 1 [SAA-664](/SAA/issues/SAA-664) — loopback-bind default, `--host` opt-in, mandatory bearer token, Origin rejection.)*
*   [x] Remove the `JIRA__PROJECT_KEY` single-project restriction so one server can operate across projects/spaces of a Jira instance. *(Outcome: `JIRA_PROJECT_KEYS` multi-project allowlist with the renamed `JIRA_*`/`XRAY_*` env contract; Phase 3 [SAA-568](/SAA/issues/SAA-568).)*
*   [x] Implement industry-standard MCP policy with clearly displayed destructive tools (SDK tool annotations plus server-level policy enforcement). *(Outcome: SDK tool annotations plus `McpPolicyService` server-level enforcement; Phase 5 [SAA-570](/SAA/issues/SAA-570).)*
*   [x] CLI installs skills for the various harnesses via symlinks to md files decrypted at install time inside the installed `@decaf-ts/with-ai` package (decryption mechanism ported from `./mcp-server`). *(Outcome: `with-ai/src/skills/SkillInstaller.ts`; Phase 2 [SAA-567](/SAA/issues/SAA-567).)*
*   [x] New encryption uses decaf-ts crypto libraries (`@decaf-ts/crypto` `CryptoService.encrypt/decrypt`), replacing raw node crypto. *(Outcome: `with-ai/src/crypto/EncryptionService.ts` on `@decaf-ts/crypto` `CryptoService` — AES-256-GCM + PBKDF2, JSON envelope; `decaf with-ai encrypt-assets`.)*
*   [x] Production build publishes a valid public npm package with ALL AI-value content (agents/skills; prompts removed per the 2026-09-02 amendment) encrypted; only decryption-key holders can use the CLI and decrypt/link skills. *(Outcome: package conformance verified in Phase 5 [SAA-570](/SAA/issues/SAA-570). Operational caveat: `dist/content` currently carries a synthetic verification key — a real out-of-band `ENCRYPTION_KEY` is required before any decrypt-sensitive release build, §6.)*
*   [x] Repo-access users can boot and see/edit skills in cleartext; encrypted artifacts are never committed (gitignored). *(Outcome: plaintext markdown stays committed; generated `.enc` outputs are gitignored and never committed.)*
*   [x] Testing: TypeScript suites against the code plus inspector verification against the production-grade artifact (same mechanism as mcp-server); Xray live tests created but run separately (user injects credentials manually); all live Jira/Xray tests idempotent and self-reverting with a loud user-facing warning on cleanup failure; complete variant coverage against as few live issues as possible. *(Outcome: unit/dist/inspector suites plus live suites under `with-ai/tests` and `with-ai/live` (Phase 6 [SAA-571](/SAA/issues/SAA-571)); live jira suite green with self-reversion evidence, QA-verified; xray live suite created but NOT run — deferred by user direction 2026-09-04 until xray-capable credentials are introduced, §6.)*
*   [x] **No commands and no prompts in with-ai (user amendment, 2026-09-02):** `with-ai/workdocs/prompts/**` and all of `with-ai/workdocs/templates/**` are removed from the repository together with every reference to them; legacy-system support may be re-added later if needed. The live `with-ai/agents/` (Paperclip company definitions) and `with-ai/skills/` are out of scope and remain untouched. Removal is implemented under [SAA-614](/SAA/issues/SAA-614). *(Outcome: removal delivered under [SAA-614](/SAA/issues/SAA-614); with-ai exposes no prompts and no commands.)*
*   [x] **Single source of truth for ticket templates (user amendment, 2026-09-02):** `with-ai/skills/common/maintain-domain-docs/assets/{bug,incident,release,specification,test}-template.md`; `jira-ticket-create` reads templates from the (decrypted) installed skills assets, with no duplicate ticket-template copies under `with-ai/workdocs`. *(Outcome: delivered — `jira-ticket-create` resolves the five ticket templates from the decrypted installed skills assets; no workdocs duplicates remain.)*

## 3. User Stories / Requirements

*   **US-1:** As a **decaf developer**, I run one detached CLI command to boot an MCP server exposing jira/xray tools for my AI harness, and clients are created per request so missing credentials never break boot.
*   **US-2:** As a **decaf developer**, I use `decaf <command>` for all other with-ai functionality without the MCP server in the picture.
*   **US-3:** As an **AI harness user**, I install decaf skills into my harness via a CLI command that symlinks decrypted markdown from the installed package, gated on holding the decryption key.
*   **US-4:** As a **multi-project Jira user**, I work across projects/spaces in one Jira instance without a single-project env restriction.
*   **US-5:** As an **operator**, I see destructive tools clearly flagged and policy-gated per the MCP industry standard before an AI client can invoke them.
*   **US-6:** As a **package consumer without repo access**, I install the public npm package and use the CLI/MCP with encrypted AI content only key holders can decrypt.
*   **US-7:** As a **repo contributor**, I see and edit skills in cleartext locally; encrypted artifacts are never committed.
*   **US-8:** As a **maintainer**, I add a new MCP domain block (tools/templates — no prompts; amended 2026-09-02) that self-registers against the MCP instance without touching core wiring.
*   **Req-1:** The system must organize all new code under `src/cli` (cli modules automatically picked up by the decaf cli) and `src/mcp/{jira,xray,common}` (tools/templates per module — no prompts; amended 2026-09-02).
*   **Req-2:** Each MCP module (jira, xray, common) must self-register its tools/templates against the MCP server instance — adding a new module must require no core changes.
*   **Req-3:** No content can be duplicated — e.g. jira tooling must read decrypted templates from the installed package, not from copies. The single source of truth for ticket templates is `with-ai/skills/common/maintain-domain-docs/assets/{bug,incident,release,specification,test}-template.md`; `jira-ticket-create` reads them from the (decrypted) installed skills assets — no duplicate ticket-template copies under `with-ai/workdocs` (amended 2026-09-02).
*   **Req-4:** The decaf logger must be configured stderr-only at MCP boot under stdio transport; stdout is reserved for MCP JSON-RPC communication.
*   **Req-5:** All decaf conventions apply throughout: `@service()` services, `logCtx(args, method, true)` contextual logging, contextual args, decaf errors only, zero `console.*`, async/await.
*   **Req-6:** Encrypted artifacts are never committed — gitignore must cover generated encrypted outputs in `with-ai` (mirroring the legacy `/src/**/*.enc` policy); plaintext markdown stays committed in the repo.
*   **Req-7:** Secrets (Jira/Xray credentials, `ENCRYPTION_KEY`) remain out-of-band; never logged, never committed.
*   **Req-8:** A legacy-functionality inventory is an explicit deliverable: every legacy tool/prompt/resource/module mapped to its new home or an explicitly justified replacement.
*   **Req-9 (user amendment, 2026-09-02):** `with-ai/workdocs/prompts/**` and all of `with-ai/workdocs/templates/**` (ados, workflow incl. commands, agent-personas, jira/ticket-templates) are removed from the repository along with every reference to them; legacy-system support may be re-added later if needed. The live `with-ai/agents/` (Paperclip company definitions) and `with-ai/skills/` are out of scope and remain untouched. Removal is implemented under [SAA-614](/SAA/issues/SAA-614); this specification records the approved content only.

## 4. Architecture & Design

Approved technical governance direction (CTO, 2026-09-02), grounded in a direct scan of both modules:

- **Package shape:** `with-ai` becomes a decaf-cli module package: `src/cli/*` commands auto-picked-up by `@decaf-ts/cli`, plus the detached MCP surface at `src/mcp/{jira,xray,common}`. Modular registration is preserved and formalized from the legacy `modules/jira/register.ts` pattern — tools/templates only, no prompt registration (user amendment, 2026-09-02).
- **MCP runtime:** boot is its own CLI command; the MCP runtime is completely detached from the remaining CLI — no shared process-lifecycle state; MCP code must not import CLI command modules. Clients (Jira/Xray) are constructed **per tool request** with lazily-loaded credentials (user hard requirement); boot must never fail on missing/invalid credentials.
- **Jira surface:** preserve auth-providers, rate-limiting, and the ADF<->markdown conversion pipeline from the legacy `jira` module (~17 zod tool-input schemas, XrayClient, ticket-create flow); remove the `JIRA__PROJECT_KEY` restriction for multi-project access (exact config shape pending the user's answer).
- **Encryption:** replace the legacy `bin/obfuscate-prompts.cjs` (raw node crypto: `scryptSync` + AES-256-GCM with an out-of-band `ENCRYPTION_KEY`) with `@decaf-ts/crypto` `CryptoService.encrypt/decrypt`. Build-time encryption of all AI-value content for the public artifact; `.enc` outputs gitignored and never committed.
- **Skill installation:** the CLI installs skills for the various harnesses by symlinking to md files decrypted at install time inside the installed `@decaf-ts/with-ai` package.
- **MCP policy:** SDK tool annotations (`readOnlyHint`, `destructiveHint`, `idempotentHint`, `openWorldHint`) with server-level policy enforcement; destructive tools clearly displayed.
- **Testing strategy:** TS-level suites against the code; MCP inspector testing against the production-grade artifact (same mechanism as legacy `test:dist-assets` / `server:inspect*`); live Jira/Xray tests idempotent and self-reverting (create → verify → delete) with loud user-facing cleanup-failure warnings; maximum variant coverage against as few live issues as possible; Xray live tests created but run separately (user introduces credentials manually).
- **Feasibility:** the port is substantial but bounded (legacy `@decaf-ts/mcp-server` v1.15.2: ~187 TS files + ~431 asset files across `jira`, `agent` (GOAP + mistreevous behavior-tree runtime), `summarization`, `template-module` modules, tools, resources, prompts; target `@decaf-ts/with-ai` v3.10.17: minimal TS surface `src/bin/cli.ts` + `src/namespace/*` plus the authoritative markdown catalog). Recommended phased decomposition: foundation (CLI + crypto + build), jira module, xray module, common + registration, tests + inspector.

**Governance conditions (binding):**

1. The open user questions (§6) remain unresolved questions until the user answers; record the answers as decisions when they land — do not bake in assumptions before then. (The 2026-09-02 scope amendment resolved the prompt/command portion of module disposition; the remaining §6 items stay open until answered.)
2. Every implementation child created under the domain root must carry specification `DECAF-51`.
3. TS-level tests plus inspector tests against the production artifact are acceptance criteria, not nice-to-haves.

## 5. Tasks Breakdown

This specification is broken down into the following tasks. Each task should be small enough to be planned and executed separately.

| ID           | Task Name                            | Priority | Status  | Dependencies |
|:-------------|:-------------------------------------|:---------|:--------|:-------------|
| Phase 1 | Legacy inventory + catalog coverage ([SAA-566](/SAA/issues/SAA-566)) | Medium | Done (snapshot 2026-09-04) | — |
| Phase 2 | CLI core + detached MCP boot + crypto pipeline + skill installer ([SAA-567](/SAA/issues/SAA-567)) | Medium | Done (snapshot 2026-09-04) | Phase 1 |
| Phase 3 | jira module — multi-project allowlist + per-request clients ([SAA-568](/SAA/issues/SAA-568)) | Medium | Done (snapshot 2026-09-04) | Phase 2 |
| Phase 4 | xray module ([SAA-569](/SAA/issues/SAA-569)) | Medium | Done (snapshot 2026-09-04) | Phase 2 |
| Phase 5 | common module + MCP policy + package conformance ([SAA-570](/SAA/issues/SAA-570)) | Medium | Done (snapshot 2026-09-04) | Phases 3–4 |
| Phase 6 | TS + inspector test suites + live idempotent tests ([SAA-571](/SAA/issues/SAA-571)) | Medium | Done (snapshot 2026-09-04) | Phase 5 |
| Amendment | Prompts/templates removal + single-sourced ticket templates ([SAA-614](/SAA/issues/SAA-614); amendment recorded under [SAA-615](/SAA/issues/SAA-615)) | Medium | Done (snapshot 2026-09-04) | — |
| Commit gates | Gate cycles: initial gates [SAA-655](/SAA/issues/SAA-655)/[SAA-656](/SAA/issues/SAA-656)/[SAA-657](/SAA/issues/SAA-657)/[SAA-658](/SAA/issues/SAA-658) → fix round 1 [SAA-664](/SAA/issues/SAA-664) → re-gates [SAA-665](/SAA/issues/SAA-665)/[SAA-666](/SAA/issues/SAA-666)/[SAA-667](/SAA/issues/SAA-667)/[SAA-668](/SAA/issues/SAA-668) → fix round 2 [SAA-675](/SAA/issues/SAA-675) → final re-gates [SAA-676](/SAA/issues/SAA-676)/[SAA-677](/SAA/issues/SAA-677)/[SAA-678](/SAA/issues/SAA-678) | Medium | Done — all final gates green (snapshot 2026-09-04) | Phases 1–6 |
| Reopened scope 1 | mcp-server parity port (vs `mcp-server@afd9dfa`) + 2 provisioning fixes; EncryptionService `ClientBasedService` conformance; `decaf-mcp` bin; `--preset` + global `--read-only`; AgileTest preset ([SAA-707](/SAA/issues/SAA-707)) | Medium | Done (snapshot 2026-09-05) | Phases 1–6 |
| Reopened scope 2 | Deployment-command validation (17 CLI commands) + additive `paperclip-plugin/` scaffold ([SAA-708](/SAA/issues/SAA-708)) | Medium | Done (snapshot 2026-09-05) | Reopened scope 1 |
| Reopened scope 3 | Test reorganization: unit / integration (live jira, from source) / e2e (dist via inspector) tiers; single dist build per run ([SAA-709](/SAA/issues/SAA-709); implementation [SAA-723](/SAA/issues/SAA-723); unit extension QA [SAA-710](/SAA/issues/SAA-710); fix round [SAA-726](/SAA/issues/SAA-726)) | Medium | Done (snapshot 2026-09-05) | Reopened scope 1 |
| Reopened-scope gates | Security verification of the reopened-scope delta ([SAA-728](/SAA/issues/SAA-728)) | High | Done — PASS, one low finding routed to the root (snapshot 2026-09-05) | Reopened scope 1–3 |
| Low-priority pair | `./utils` AgileTest equivalent for the xray tooling ([SAA-732](/SAA/issues/SAA-732)) + `agents` preset in the with-ai MCP ([SAA-733](/SAA/issues/SAA-733)) | Low | Delivered — uncommitted, pending the next commit gate (snapshot 2026-09-05) | Reopened scope 1–3 |
| Follow-up | Source-JSDoc coverage pass B3–B7 ([SAA-669](/SAA/issues/SAA-669)) | Low | Open (non-blocking) | — |
| Follow-up | CLI/MCP usage docs + stale `workdocs/4-Description.md` ([SAA-670](/SAA/issues/SAA-670)) | Low | Open (non-blocking) | — |

Statuses above are Paperclip snapshots taken 2026-09-04 (Phases 1–6, Amendment, Commit gates) and 2026-09-05 (Reopened scope 1–3, Reopened-scope gates, Low-priority pair) during the respective completion milestones; Paperclip remains authoritative for task status.

## 6. Open Questions / Risks

*   **Module disposition (resolved — completion fold-in, 2026-09-04):** legacy prompt/command modules are **dropped, not ported** — with-ai offers no prompts and no commands, the MCP prompt registration is removed, and `with-ai/workdocs/prompts/**` plus all of `with-ai/workdocs/templates/**` are deleted from the repository (implementation under [SAA-614](/SAA/issues/SAA-614)). The remaining non-Jira TS modules (agent GOAP runtime, summarization, decoration tools, server-info/repo-metadata) are **dropped with explicit dispositions** in the Req-8 legacy-functionality inventory (`with-ai/workdocs/legacy-inventory.md`); the coverage validation is delivered and concludes "no uncovered capability remains" — nothing user-facing is lost, measured at the capability level.
*   **Jira multi-project config shape (resolved — delivered in Phase 3, [SAA-568](/SAA/issues/SAA-568)):** the `JIRA__PROJECT_KEY` single-project lock is replaced by the `JIRA_PROJECT_KEYS` multi-project allowlist with a renamed `JIRA_*`/`XRAY_*` env contract; clients are constructed per request against the allowlisted projects.
*   **Package identity / deprecation (partially resolved — residual operational question):** `@decaf-ts/with-ai` continues as the publishing identity; legacy `@decaf-ts/mcp-server` is retired from the workspace submodule manifest and nothing in the monorepo depends on it, but the npm deprecation/publish disposition of the legacy package name (final publish + deprecation notice vs. silent drop) remains an open operational question.
*   **Decryption-key distribution (unresolved — launch-blocking operational decision):** the key is required for CLI skill decrypt/install; the distribution model (out-of-band vs. mechanism) is still open. Operational note (2026-09-04): `dist/content` currently carries a **synthetic verification key** from Phase 5 — a real out-of-band `ENCRYPTION_KEY` is required before any decrypt-sensitive release build.
*   **Deferred — xray live suites (user direction, 2026-09-04):** the xray live suite is created but NOT run; the current account does not support xray. It is to be run separately when xray-capable credentials are introduced.
*   **Reopened-scope low-priority pair (deferral superseded — delivered 2026-09-05, documentation milestone [SAA-750](/SAA/issues/SAA-750)):** the user's original 2026-09-05 direction (start only after reopened-scope items 1–7 are done **and** a commit has been requested on the root; no implementation children created) is superseded — both items are implemented and done under [SAA-732](/SAA/issues/SAA-732)/[SAA-733](/SAA/issues/SAA-733) and delivered **uncommitted** in the worktree, pending the next commit gate on the root (§7). The `agents` preset supersedes the [SAA-615](/SAA/issues/SAA-615) "no prompts in with-ai" amendment **for the `agents` preset only** (§7).
*   **L1 (low, security gate [SAA-728](/SAA/issues/SAA-728), 2026-09-05):** `bin/tag-release.sh:215-222` — the npm-publish default flipped: the default path no longer consumes `.npmtoken`; token-based publishing now requires `NPM_PUBLISH_INTERACTIVE=0`, while the adjacent comment states the opposite. Security-neutral-to-positive (fail-closed: without stored npm auth the publish fails rather than silently reading a token file), but a behavior change to a script deployments still use plus a false comment that will mislead the next operator. Remediation routed to the domain root: correct the comment and confirm the release runbook sets `NPM_PUBLISH_INTERACTIVE=0` wherever token publishing is required.
*   **Risk (retired):** the port is large (~187 TS files, ~431 assets) — retired by the delivered legacy-functionality inventory (Req-8): "no functionality lost" is proven at the capability level.
*   **Risk (retired):** live-instance test noise and cleanup failures — mitigated by the idempotent self-reverting test bar and loud cleanup-failure warnings; the live jira suite ran green with self-reversion evidence (QA-verified). Residual: the xray live suite is deferred (above) and will need the same self-reversion discipline when run.
*   **Risk (retired):** stdout pollution would break stdio MCP transport — mitigated by the stderr-only logger constraint (Req-4), implemented via `with-ai/src/mcp/StderrLogging.ts`.

## 7. Results & Artifacts

Completion record (finalized 2026-09-04 from the CTO completion handoff on the domain root; executor and QA evidence recorded as reported — not re-run by the documentation pass):

*   **Artifacts (delivered):**
    *   `with-ai/src/cli/**` — decaf-cli command modules (`mcp`, `encrypt-assets`, `install-skills`)
    *   `with-ai/src/mcp/**` — `McpBoot`, `McpServerService`, `McpModuleRegistry`, `McpPolicyService`, `McpContentResolver`, `RequestClientFactory`, `StderrLogging`, and the `jira`/`xray`/`common` modules
    *   `with-ai/src/crypto/**` — `@decaf-ts/crypto`-based `EncryptionService`
    *   `with-ai/src/skills/**` — `SkillInstaller` (symlinks to decrypted md)
    *   `with-ai/tests/**`, `with-ai/live/**`, `with-ai/jest.config.cjs` — unit/dist/inspector/live suites and config
    *   `with-ai/workdocs/legacy-inventory.md` — 100% legacy-surface inventory (Req-8)
    *   `with-ai/package.json` — docs script fix + `jsdoc-mermaid` devDependency
    *   `workdocs/ai/project/specifications/DECAF_51.md`, `workdocs/ai/project/plan.md` — this record + plan
    *   `workdocs/ai/project/technical-docs/architecture-handbook/09-tooling-infra.md` — mcp/cli chapters ([SAA-658](/SAA/issues/SAA-658)/[SAA-668](/SAA/issues/SAA-668))
*   **Acceptance criteria:** all §2 goals are checked with outcomes; both user-approved scope amendments (no prompts/commands; single-sourced ticket templates) are implemented, and the per-request-client hard requirement is honored and test-pinned.
*   **Commit-gate cycles:** initial four gates ([SAA-655](/SAA/issues/SAA-655)/[SAA-656](/SAA/issues/SAA-656)/[SAA-657](/SAA/issues/SAA-657)/[SAA-658](/SAA/issues/SAA-658)) → fix round 1 ([SAA-664](/SAA/issues/SAA-664): S1–S5 security hardening incl. http loopback-default + `--host` opt-in + mandatory bearer token + Origin rejection; D1–D3 docs pipeline; J1–J2 JSDoc; P1 prompts removal) → re-gates ([SAA-665](/SAA/issues/SAA-665)/[SAA-666](/SAA/issues/SAA-666)/[SAA-667](/SAA/issues/SAA-667)/[SAA-668](/SAA/issues/SAA-668)) → fix round 2 ([SAA-675](/SAA/issues/SAA-675): 11 JSDoc tags, N2 nested error sanitization, N1 token redaction, N3 xray auth tests, N4 http error listener + port validation, R6 defaults pinning) → final re-gates all green ([SAA-676](/SAA/issues/SAA-676) security: all fixed, no new issues; [SAA-677](/SAA/issues/SAA-677) docs: `npm run docs` exit 0 zero errors; [SAA-678](/SAA/issues/SAA-678) QA: re-sign-off, no findings).
*   **Verification evidence (as reported by the executors and QA):**
    *   `npm run build` — exit 0 (post-round-2, [SAA-676](/SAA/issues/SAA-676)/[SAA-678](/SAA/issues/SAA-678))
    *   `npm run test:unit` — 257 passed / 0 failed / 7 skipped (dist-assets gated)
    *   `npm run test:all` — 270 passed / 0 failed / 7 skipped; live suites correctly excluded from collection
    *   `NODE_ENV=development npm run docs` — exit 0, zero jsdoc ERROR lines ([SAA-677](/SAA/issues/SAA-677))
    *   Live jira suite — green with self-reversion evidence ([SAA-571](/SAA/issues/SAA-571), QA-verified); xray live deferred per user direction (§6)
    *   Security — three-pass chain ending: all findings fixed, no new issues ([SAA-656](/SAA/issues/SAA-656) → [SAA-665](/SAA/issues/SAA-665) → [SAA-676](/SAA/issues/SAA-676))
    *   QA sign-off chain: [SAA-657](/SAA/issues/SAA-657) → [SAA-667](/SAA/issues/SAA-667) → [SAA-678](/SAA/issues/SAA-678) (re-sign-off, no findings)
*   **Delivery state:** the user committed the bulk of the port himself on `with-ai` master (through `fc0b566`); the domain root's single user-approved commit covers the remaining uncommitted DECAF-51 delta. The working tree also holds unrelated content that is explicitly excluded from this ticket's staging: `with-ai/agents/inbox-watcher/**`, `bin/configure-routines.mjs`, `bin/configure-tool-gateways.mjs`, `bin/generate-agent-mcp-config.mjs`, `bin/tool-schemas/`, `docker/paperclip/*.yaml`, `with-ai/AGENTS.md`, staged-then-deleted `src/mcp/jira/JiraContentResolver.ts` (net: file must not exist), and root `workdocs/ai/project/specifications/DECAF_52.md` (belongs to [SAA-650](/SAA/issues/SAA-650)).
*   **Residuals / follow-ups (explicit, non-blocking):** [SAA-669](/SAA/issues/SAA-669) source-JSDoc coverage pass (B3–B7); [SAA-670](/SAA/issues/SAA-670) CLI/MCP usage docs + stale `workdocs/4-Description.md`; xray live suites deferred until xray-capable credentials exist (§6); real out-of-band `ENCRYPTION_KEY` for decrypt-sensitive release builds (§6); npm deprecation/publish disposition of the legacy `@decaf-ts/mcp-server` package name (§6).

*   **Phase-2 completion fold-in (2026-09-05 — reopened scope, items 1–7; documentation milestone [SAA-729](/SAA/issues/SAA-729) on evidence from the CTO):** the user's reopened-scope comment on the domain root (2026-09-05, [comment 54a85ebd](/SAA/issues/SAA-560#comment-54a85ebd-4c57-4a45-8a70-e5d9f19a3ebe)) defined seven priority items; all seven are delivered and verified (executor and QA evidence recorded as reported — not re-run by the documentation pass):
    1.  **mcp-server parity port** — full parity vs `mcp-server@afd9dfa` verified (no functional regressions); two provisioning bugs fixed that referenced the retired `@decaf-ts/mcp-server` and legacy env vars ([SAA-707](/SAA/issues/SAA-707)).
    2.  **EncryptionService service-rule conformance** — now a `ClientBasedService` subclass, boot-gated, `logCtx` conformance, no side-effect `service()(CryptoService)` DI ([SAA-707](/SAA/issues/SAA-707)).
    3.  **`decaf-mcp` bin** — `package.json` bin entry correct; builds with the right shebang ([SAA-707](/SAA/issues/SAA-707)).
    4.  **Presets + global `--read-only`** — `--preset`/`--read-only` wired through `decaf with-ai mcp`; central `selectModules` + `registerTool` read-only filtering in `McpServerService` ([SAA-707](/SAA/issues/SAA-707)).
    5.  **Deployment-command validation + paperclip plugin migration** — all 17 CLI commands validated (exists + smoke); `paperclip-plugin/` scaffold added additively with no deletions or behavior changes of scripts deployments still use ([SAA-708](/SAA/issues/SAA-708)).
    6.  **AgileTest preset** — `src/mcp/agiletest/**` module registered like xray (free xray analog) ([SAA-707](/SAA/issues/SAA-707)). The `./utils` xray-tooling AgileTest equivalent is the user-deferred low-priority pair (§6) — not part of this milestone.
    7.  **Test reorganization** — unit / integration (live jira, from source) / e2e (dist via inspector) tiers; single production dist build per run (`test:full` = one `build:prod` then all tiers); idempotent self-reverting live tests with a manual-action warning path ([SAA-709](/SAA/issues/SAA-709); implementation [SAA-723](/SAA/issues/SAA-723); unit extension QA'd under [SAA-710](/SAA/issues/SAA-710)).
    *   **Verification evidence (latest runs, 2026-09-05, as reported):** `npm run build` (dev) exit 0; `npx tsc --noEmit` exit 0; eslint clean on touched files ([SAA-707](/SAA/issues/SAA-707)). Unit: 284 passed / 7 skipped (credential-gated, opt-in) / 0 failed (post-[SAA-726](/SAA/issues/SAA-726) fix round, QA-verified on [SAA-709](/SAA/issues/SAA-709)). Integration live-jira from source: 11/11 including the offline manual-cleanup warning-path proof. E2e dist via inspector: 10/10 booting `lib/cjs/bin/mcp.cjs` over MCP stdio with binary mtime unchanged (no rebuild). Revert safety: both live tiers delete canonical + shadow issues and prove deletion via failing follow-up reads; independent live-instance search for `[with-ai live]` residue: zero. Production bug fixed this phase: `src/mcp/jira/adf/sanitizeDoc.ts` panel-node unwrap (content preserved, type removed).
    *   **Security gate ([SAA-728](/SAA/issues/SAA-728), 2026-09-05): PASS — no security regressions in the reopened-scope delta.** Fourth-pass verification of the full phase-2 worktree delta (48 unstaged modified + 6+ untracked files, plus the unchanged staged prior-phase package) against the [SAA-676](/SAA/issues/SAA-676) clean baseline. All six focus areas PASS: EncryptionService conformance is fail-closed (boot-gating cannot degrade to no-op encryption; no key/plaintext leakage via `logCtx`); `--preset`/`--read-only` enforcement is central and default-deny at `McpServerService.registerTool` with no bypass path (all 26 tools' annotations audited; unknown presets fail closed); the AgileTest module has full parity with the reviewed xray hardening (per-request clients, env-var names only in errors, missing credentials never break boot); the sanitizeDoc panel-unwrap preserves the stripped-type set (verified 56/56 AdfConversion + JiraToolHandlers); the paperclip-plugin scaffold spawns legacy scripts via argv arrays from a static path registry with secret-free output; live test tiers are properly gated (`TEST_LIVE`/credential envs; no secrets in fixtures). One low finding **L1** (`bin/tag-release.sh` npm-publish default flip + false comment — §6) is routed to the domain root for remediation; informational notes (no action): `--preset` passed without a value yields an unclean `TypeError` (robustness nit), the pre-existing `ENCRYPTION_KEY` plain value in `configure-tool-gateways.ts` (accepted [SAA-656](/SAA/issues/SAA-656) residual, not a phase-2 regression), and the dev-only `@decaf-ts/mcp-server` devDep bump. Gate's own runs: `npx tsc --noEmit` exit 0; `npm run test:unit` 284 passed / 7 skipped / 0 failed; AdfConversion + JiraToolHandlers 56/56; paperclip-plugin vitest 10/10. Residual risk unchanged from the accepted set.
    *   **Commit gate (re-entered after this fold-in):** the root returns to the user-approved single-commit gate on [SAA-560](/SAA/issues/SAA-560). Prior state: the user committed the bulk of the port himself on `with-ai` master; the staged DECAF-51 diff (111 files / 61013 insertions) plus the unstaged reopened-scope edits remain uncommitted; the user previously chose self-commit and deferred. Commit-time note from QA ([SAA-709](/SAA/issues/SAA-709)): `live/jira-live.test.ts` is staged but deleted in the worktree — the commit must `git add -A` so the moved file does not land in the commit.
    *   **Deferred follow-ups (user direction, 2026-09-05 — no issues created):** the `./utils` xray-tooling AgileTest equivalent and the `agents` preset prompts start only after items 1–7 are done **and** a commit has been requested (§6).

*   **Landed-commit + low-priority-pair fold-in (2026-09-05 — documentation milestone [SAA-750](/SAA/issues/SAA-750), amending the phase-2 fold-in above):**
    *   **Landed commits (created 2026-09-05, verified in the repository, not pushed):** the previously pending single user-approved commit of the DECAF-51 delta landed as two commits:
        *   with-ai submodule `c9fde50` — "DECAF-51: rebuild mcp-server as with-ai mcp/cli with encrypted content pipeline" (147 files, 12009 insertions, 2203 deletions): the phase-1+2 package — jira/xray/agiletest modules, presets + global `--read-only`, EncryptionService conformance, `decaf-mcp` bin, `paperclip-plugin/` scaffold, 3-tier tests (per the phase-2 fold-in above).
        *   repo root `293ab32` — "DECAF-51: with-ai mcp/cli design and delivery documentation + submodule pin" (6 files: this record's phase-2 fold-in, `plan.md`, tooling design docs — `09-tooling-infra.md`, `11-tooling-mcp-design.md`, architecture-handbook README — and the with-ai submodule pin at `c9fde50`).
        *   This resolves the commit-gate state recorded in the phase-2 fold-in above (staged diff plus unstaged reopened-scope edits pending the single user-approved commit), including the QA commit-time note ([SAA-709](/SAA/issues/SAA-709)).
    *   **Low-priority pair — delivered, uncommitted (pending the next commit gate on the root):**
        *   [SAA-732](/SAA/issues/SAA-732) — `./utils` AgileTest equivalent for the xray tooling: provider-agnostic `src/tests/jestTestImporter.ts` shared by the Xray and new AgileTest teardowns (`runJestAgileTestTeardown()`), plus shared reported-test helpers (`src/tests/reporter.ts`, `src/tests/jest.ts`).
        *   [SAA-733](/SAA/issues/SAA-733) — `agents` preset in the with-ai MCP (`src/mcp/agents/`, `optInOnly`): dynamic per-paperclip-agent prompts built at runtime from the decrypted per-agent bundles — AGENTS.md + SOUL.md + TOOLS.md concatenation, cross-agent mention rewriting to the `with-ai-agent-<slug>` prompt names, and `paperclip` substring stripping — registered only via `--preset agents`. **This supersedes the [SAA-615](/SAA/issues/SAA-615) "no prompts in with-ai" amendment for the `agents` preset only:** that preset exposes prompts; tools and templates remain prompt-free, and every other preset (and the default surface) still exposes zero prompts.
    *   **Scope note:** remaining uncommitted worktree content outside the DECAF-51 delta is out of scope for this record and this delivery.
    *   **Commit gate:** the root re-enters the user-approved single-commit gate on [SAA-560](/SAA/issues/SAA-560) for the low-priority-pair delta above; the documentation amendment itself (this fold-in and the `plan.md` mirror) is likewise left uncommitted for that commit.
